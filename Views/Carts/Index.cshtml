@model List<dynamic>

@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <table class="table table-striped table-hover">
        <thead class="thead-dark">
            <p>
                <a asp-action="Upsert">Create New</a>
            </p>
            <tr>
                <th>Product Name</th>
                <th>Category Name</th>
                <th>Price</th>
                <th>Total</th>
                <th>Quantity</th>
                <th>Measure Of Scale</th>
                <th>NewOrUsed</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.ProductName</td>
                    <td>@item.CategoryName</td>
                    <td>@item.Price</td>
                    <td>@(item.Qty * Int32.Parse(@item.Price))</td>
                    <td>
                    <input type="hidden" name="cartItems[@item.CartId].CartId" value="@item.CartId" />
                    <input type="number" name="cartItems[@item.CartId].Qty" value="@item.Qty" />
                    </td>
                    <td>@item.MeasureOfScale</td>
                    <td>
                        @if (item.NewOrUsed)
                        {
                            <span class="badge bg-success">Yes</span>
                        }
                        else
                        {
                            <span class="badge bg-danger">No</span>
                        }
                    </td>
                    <td>
                        <a asp-action="Upsert" asp-route-id="@item.ProductId">Edit</a> |
                        <a asp-action="Delete" asp-route-id="@item.ProductId">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
